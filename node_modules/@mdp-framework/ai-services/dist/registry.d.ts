/**
 * AI Provider Registry
 *
 * Manages registration and retrieval of AIProvider implementations.
 * Supports multiple providers with default selection.
 */
import type { AIProvider, AIProviderRegistry } from './contracts/index.js';
export declare class DefaultAIProviderRegistry implements AIProviderRegistry {
    private providers;
    private defaultProvider;
    /**
     * Register a provider
     */
    register(provider: AIProvider): void;
    /**
     * Get provider by name
     */
    get(name: string): AIProvider;
    /**
     * List all registered provider names
     */
    listProviders(): string[];
    /**
     * Get the default provider
     */
    getDefault(): AIProvider;
    /**
     * Check if a provider is registered
     */
    has(name: string): boolean;
    /**
     * Set the default provider
     */
    setDefault(name: string): void;
    /**
     * Unregister a provider
     */
    unregister(name: string): void;
    /**
     * Clear all providers
     */
    clear(): void;
}
//# sourceMappingURL=registry.d.ts.map