/**
 * Component Index
 *
 * Indexes framework components with embeddings for similarity search.
 */
import type { AIProvider } from '@mdp-framework/ai-services';
import type { Component } from '../types.js';
export declare class ComponentIndex {
    private aiProvider;
    private components;
    constructor(aiProvider: AIProvider);
    /**
     * Index components for a framework
     */
    indexComponents(framework: string, components: Array<{
        name: string;
        description: string;
        location: string;
    }>): Promise<void>;
    /**
     * Get components for a framework
     */
    getComponents(framework: string): Component[];
    /**
     * Check if framework is indexed
     */
    hasFramework(framework: string): boolean;
    /**
     * Clear index
     */
    clear(): void;
    /**
     * Get all indexed frameworks
     */
    listFrameworks(): string[];
}
//# sourceMappingURL=component-index.d.ts.map