/**
 * Threshold Checker
 *
 * Determines if similarity scores meet the threshold for component existence.
 */
import type { SimilarityResult } from '../types.js';
export declare class ThresholdChecker {
    /**
     * Default threshold for similarity matching (0.8)
     */
    static readonly DEFAULT_THRESHOLD = 0.8;
    /**
     * Check if a component exists based on similarity threshold
     */
    static checkExists(result: SimilarityResult): boolean;
    /**
     * Get recommendation based on similarity score
     */
    static getRecommendation(score: number, threshold: number): 'skip_aoi' | 'create_aoi' | 'review_manual';
    /**
     * Validate threshold value
     */
    static validateThreshold(threshold: number): void;
    /**
     * Calculate confidence in similarity result
     */
    static calculateConfidence(result: SimilarityResult): number;
}
//# sourceMappingURL=threshold-checker.d.ts.map