/**
 * Vector Similarity Search
 *
 * Embedding-based similarity search for component detection.
 */
import type { AIProvider } from '@mdp-framework/ai-services';
import type { Component, SimilarityResult } from '../types.js';
export declare class VectorSearch {
    private aiProvider;
    constructor(aiProvider: AIProvider);
    /**
     * Search for similar components using embeddings
     */
    search(query: string, candidates: Component[], threshold: number): Promise<SimilarityResult>;
    /**
     * Calculate cosine similarity between two vectors
     */
    private cosineSimilarity;
}
//# sourceMappingURL=vector-search.d.ts.map