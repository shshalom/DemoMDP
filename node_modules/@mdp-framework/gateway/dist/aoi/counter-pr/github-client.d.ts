/**
 * GitHub Client
 *
 * Wrapper for GitHub API operations (branch creation, commits, PRs).
 */
import type { PRFileChange } from '../types.js';
export interface GitHubClientConfig {
    token?: string;
    client?: any;
}
export interface GitHubPR {
    number: number;
    title: string;
    body: string;
    html_url: string;
    head: {
        ref: string;
        sha: string;
        repo: {
            full_name: string;
        };
    };
    base: {
        ref: string;
    };
}
export declare class GitHubClient {
    private client?;
    constructor(config?: GitHubClientConfig);
    /**
     * Create a new branch
     */
    createBranch(repo: string, branchName: string, baseBranch?: string): Promise<string>;
    /**
     * Commit files to a branch
     */
    commitFiles(branchName: string, files: PRFileChange[], commitMessage: string, repo?: string): Promise<void>;
    /**
     * Create a pull request
     */
    createPullRequest(config: {
        repo: string;
        head: string;
        base: string;
        title: string;
        body: string;
        reviewers?: string[];
    }): Promise<GitHubPR>;
    /**
     * Check if client is configured
     */
    isConfigured(): boolean;
}
//# sourceMappingURL=github-client.d.ts.map