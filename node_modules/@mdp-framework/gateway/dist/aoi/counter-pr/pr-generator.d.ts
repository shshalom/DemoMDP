/**
 * Counter-PR Generator
 *
 * Generates code and creates pull requests in framework repositories.
 */
import type { AIProvider } from '@mdp-framework/ai-services';
import type { AOISpec, PRFileChange } from '../types.js';
export declare class CounterPRGenerator {
    private aiProvider;
    constructor(aiProvider: AIProvider);
    /**
     * Generate component implementation code
     */
    generateImplementation(spec: AOISpec, includeTests: boolean): Promise<PRFileChange[]>;
    /**
     * Generate component code using LLM
     */
    private generateComponentCode;
    /**
     * Generate test code using LLM
     */
    private generateTestCode;
    /**
     * Build implementation prompt
     */
    private buildImplementationPrompt;
    /**
     * Build test prompt
     */
    private buildTestPrompt;
    /**
     * Extract code from LLM response (remove markdown fences)
     */
    private extractCode;
    /**
     * Infer component file path based on framework
     */
    private inferComponentPath;
    /**
     * Infer test file path based on framework
     */
    private inferTestPath;
    /**
     * Infer programming language from framework
     */
    private inferLanguage;
}
//# sourceMappingURL=pr-generator.d.ts.map