/**
 * Notification Dispatcher
 *
 * Dispatches AOI notifications to Jira, Slack, and other platforms.
 */
import type { AOISpec, AOIAction } from '../types.js';
import type { PolicyCard } from '@mdp-framework/schemas';
export declare class NotificationDispatcher {
    private config?;
    constructor(config?: {
        jiraEnabled?: boolean;
        slackEnabled?: boolean;
        jiraClient?: any;
        slackClient?: any;
    } | undefined);
    /**
     * Dispatch notifications for an AOI spec
     */
    dispatch(spec: AOISpec, policy: PolicyCard): Promise<AOIAction[]>;
    /**
     * Create Jira issue (implementation placeholder)
     */
    private createJiraIssue;
    /**
     * Send Slack notification (implementation placeholder)
     */
    private sendSlackNotification;
    /**
     * Build Slack message for AOI notification
     */
    private buildSlackMessage;
    /**
     * Create simulated issue action (for testing)
     */
    private createSimulatedIssueAction;
    /**
     * Create simulated notify action (for testing)
     */
    private createSimulatedNotifyAction;
    /**
     * Create failed action
     */
    private createFailedAction;
}
//# sourceMappingURL=notification-dispatcher.d.ts.map