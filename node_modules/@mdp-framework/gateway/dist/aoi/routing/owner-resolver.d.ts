/**
 * Owner Resolver
 *
 * Resolves component owners from policy configuration and ownership policies.
 */
import type { PolicyCard } from '@mdp-framework/schemas';
import type { AOISpec } from '../types.js';
export declare class OwnerResolver {
    private mcpClient?;
    constructor(mcpClient?: any | undefined);
    /**
     * Resolve owners for an AOI spec
     */
    resolveOwners(spec: AOISpec, policy: PolicyCard): Promise<string[]>;
    /**
     * Match owners by framework pattern
     */
    private matchOwnersByFramework;
    /**
     * Check if framework matches pattern
     */
    private matchesPattern;
}
//# sourceMappingURL=owner-resolver.d.ts.map