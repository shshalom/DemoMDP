/**
 * Follow-up PR Creator
 *
 * Creates follow-up PRs to migrate original code to use framework components.
 */
import type { AOISpec, PullRequest, AOIAction } from '../types.js';
import type { GitHubClient } from '../counter-pr/github-client.js';
import type { AIProvider } from '@mdp-framework/ai-services';
export declare class FollowupPRCreator {
    private aiProvider;
    private githubClient;
    private migrationGenerator;
    constructor(aiProvider: AIProvider, githubClient: GitHubClient);
    /**
     * Create follow-up PR to migrate code
     */
    createFollowupPR(originalPR: PullRequest, counterPR: PullRequest, spec: AOISpec): Promise<AOIAction>;
    /**
     * Build follow-up PR description
     */
    private buildFollowupPRDescription;
}
//# sourceMappingURL=pr-creator.d.ts.map