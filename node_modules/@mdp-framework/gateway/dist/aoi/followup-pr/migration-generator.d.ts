/**
 * Migration Generator
 *
 * Generates code migrations to replace custom code with framework components.
 */
import type { AIProvider } from '@mdp-framework/ai-services';
import type { AOISpec, PullRequest, PRFileChange } from '../types.js';
export declare class MigrationGenerator {
    private aiProvider;
    constructor(aiProvider: AIProvider);
    /**
     * Generate migration code to replace custom implementation with framework component
     */
    generateMigration(originalPR: PullRequest, spec: AOISpec): Promise<PRFileChange[]>;
    /**
     * Build migration prompt
     */
    private buildMigrationPrompt;
    /**
     * Get migration schema
     */
    private getMigrationSchema;
}
//# sourceMappingURL=migration-generator.d.ts.map