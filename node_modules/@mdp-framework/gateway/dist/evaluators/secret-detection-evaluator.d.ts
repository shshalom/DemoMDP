/**
 * Secret Detection Evaluator
 *
 * Evaluates secret detection policies (eng.security.secrets)
 * Checks for hardcoded secrets, API keys, passwords, tokens
 */
import type { PolicyCard } from '@mdp-framework/schemas/contracts';
import type { PolicyEvaluator, EvaluationContext, EvaluationResult } from './types.js';
export declare class SecretDetectionEvaluator implements PolicyEvaluator {
    readonly id = "secret-detection";
    readonly supportedPolicies: string[];
    canEvaluate(policy: PolicyCard): boolean;
    evaluate(context: EvaluationContext): Promise<EvaluationResult>;
    private getLineFromMatch;
    private getLineNumber;
    private isInComment;
    private matchesAnyGlob;
    private matchesGlob;
    private getPatternName;
}
//# sourceMappingURL=secret-detection-evaluator.d.ts.map