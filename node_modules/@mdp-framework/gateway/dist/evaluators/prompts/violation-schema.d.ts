/**
 * Violation Schema (Stream 2)
 *
 * JSON Schema for structured LLM output when evaluating policies.
 * This ensures the AI returns violations in a consistent, parseable format.
 */
import type { JSONSchema } from '@mdp-framework/ai-services';
/**
 * Schema for LLM-generated violation detection
 */
export declare const ViolationSchema: JSONSchema;
/**
 * Type for the structured output from LLM
 */
export interface StructuredViolationOutput {
    violations: Array<{
        message: string;
        severity: 'info' | 'warning' | 'error';
        location?: {
            file?: string;
            line?: number;
            column?: number;
        };
        suggestedFix?: string;
        confidence: number;
        reasoning?: string;
    }>;
    overall_assessment?: string;
}
//# sourceMappingURL=violation-schema.d.ts.map