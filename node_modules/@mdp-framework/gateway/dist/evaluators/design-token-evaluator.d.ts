/**
 * Design Token Evaluator
 *
 * Evaluates design token policies (ui.design.tokens)
 * Checks for hardcoded colors, spacing, and other design values
 */
import type { PolicyCard } from '@mdp-framework/schemas/contracts';
import type { PolicyEvaluator, EvaluationContext, EvaluationResult } from './types.js';
export declare class DesignTokenEvaluator implements PolicyEvaluator {
    readonly id = "design-token";
    readonly supportedPolicies: string[];
    private readonly hexColorPattern;
    private readonly hardcodedSpacingPattern;
    canEvaluate(policy: PolicyCard): boolean;
    evaluate(context: EvaluationContext): Promise<EvaluationResult>;
    private getLineFromMatch;
    private getLineNumber;
    private isInComment;
    private isInImport;
    private generateColorFix;
    private generateSpacingFix;
}
//# sourceMappingURL=design-token-evaluator.d.ts.map