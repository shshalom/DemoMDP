/**
 * Evaluator Registry
 *
 * Central registry for managing and dispatching policy evaluators
 */
import type { PolicyCard } from '@mdp-framework/schemas/contracts';
import type { PolicyEvaluator, EvaluatorRegistry, EvaluationContext, EvaluationResult } from './types.js';
export declare class DefaultEvaluatorRegistry implements EvaluatorRegistry {
    private evaluators;
    private defaultEvaluator;
    /**
     * Register an evaluator
     */
    register(evaluator: PolicyEvaluator): void;
    /**
     * Register a default/fallback evaluator
     * This evaluator is used when no specific evaluator is found for a policy
     *
     * @param evaluator - The evaluator to use as default (typically UniversalAgentEvaluator)
     */
    registerDefault(evaluator: PolicyEvaluator): void;
    /**
     * Get evaluator for a policy
     */
    getEvaluator(policy: PolicyCard): PolicyEvaluator | null;
    /**
     * Evaluate a policy
     */
    evaluate(policy: PolicyCard, context: Omit<EvaluationContext, 'policy'>): Promise<EvaluationResult>;
    /**
     * Evaluate multiple policies
     */
    evaluateAll(policies: PolicyCard[], context: Omit<EvaluationContext, 'policy'>): Promise<EvaluationResult[]>;
    /**
     * Get all registered evaluator IDs
     */
    getRegisteredEvaluators(): string[];
}
/**
 * Create a registry with default evaluators
 */
export declare function createEvaluatorRegistry(): EvaluatorRegistry;
//# sourceMappingURL=registry.d.ts.map