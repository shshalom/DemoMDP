/**
 * PR Comment Templates (M5 - Team B)
 *
 * Render PR comments with evidence and policy violation context.
 * Templates follow Step 11 spec for consolidated comments.
 */
import type { PolicyCard, StateCapsule } from '@mdp-framework/schemas/contracts';
interface TemplateContext {
    policy?: PolicyCard;
    violations?: Array<{
        message?: string;
        location?: {
            file?: string;
            line?: number;
            column?: number;
        };
        severity?: 'info' | 'warning' | 'error';
    }>;
    patch?: string;
    evidence?: StateCapsule['evidence'];
    message?: string;
    [key: string]: unknown;
}
/**
 * Render a PR comment template
 */
export declare function renderCommentTemplate(templateName: string, context: TemplateContext): string;
export {};
//# sourceMappingURL=index.d.ts.map