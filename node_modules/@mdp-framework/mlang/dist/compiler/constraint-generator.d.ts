/**
 * AI-powered constraint generator
 *
 * Uses AIProvider interface to generate structured constraints from policy markdown.
 * Provider-agnostic: works with Anthropic, OpenAI, or any AIProvider implementation.
 */
import type { AIProvider } from '@mdp-framework/ai-services';
export interface ConstraintGenerationOptions {
    /** AI provider to use */
    provider: AIProvider;
    /** Policy ID (for context) */
    policyId: string;
    /** Detection section from markdown (focused input for AI) */
    detectionSection?: string;
    /** Full markdown body (fallback if no detection section) */
    markdownBody: string;
    /** Model to use (optional, provider default used if not specified) */
    model?: string;
}
export interface ConstraintGenerationResult {
    /** Generated constraints */
    constraints?: Record<string, unknown>;
    /** AI confidence score (0.0-1.0) */
    confidence: number;
    /** Tokens used */
    tokensUsed: number;
    /** Cost in USD */
    costUsd: number;
    /** Error if generation failed */
    error?: string;
}
/**
 * Generate constraints from policy markdown using AI
 *
 * Focuses on Detection section for efficient token usage.
 * Returns structured constraints suitable for PolicyCard.
 */
export declare function generateConstraints(options: ConstraintGenerationOptions): Promise<ConstraintGenerationResult>;
//# sourceMappingURL=constraint-generator.d.ts.map