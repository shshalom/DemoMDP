/**
 * Policy Signer - Ed25519 signing for policy cards
 *
 * Signs compiled policy cards with organization private keys.
 * Verification uses public keys pinned in .mlang/keys/
 */
import type { PolicyCard } from '@mdp-framework/schemas/contracts';
export interface SignOptions {
    /** Path to private key file (ed25519, hex or base64) */
    keyFile?: string;
    /** Private key bytes (alternative to keyFile) */
    privateKey?: Uint8Array;
}
export interface VerifyOptions {
    /** Path to public key file (ed25519, hex or base64) */
    keyFile?: string;
    /** Public key bytes (alternative to keyFile) */
    publicKey?: Uint8Array;
}
export interface SignedPolicyCard extends Omit<PolicyCard, 'signature'> {
    hash: string;
    signature: string;
}
/**
 * Sign a policy card with ed25519 private key
 */
export declare function signPolicy(card: Omit<PolicyCard, 'hash' | 'signature'>, options: SignOptions): Promise<SignedPolicyCard>;
/**
 * Verify a signed policy card with ed25519 public key
 */
export declare function verifyPolicy(card: PolicyCard, options: VerifyOptions): Promise<{
    ok: boolean;
    error?: string;
}>;
/**
 * Generate a new ed25519 key pair
 */
export declare function generateKeyPair(): Promise<{
    privateKey: Uint8Array;
    publicKey: Uint8Array;
}>;
/**
 * Save key pair to files (hex format)
 */
export declare function saveKeyPair(privateKey: Uint8Array, publicKey: Uint8Array, basePath: string): Promise<void>;
//# sourceMappingURL=signer.d.ts.map