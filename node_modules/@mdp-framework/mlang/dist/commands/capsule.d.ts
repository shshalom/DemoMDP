/**
 * Capsule Store CLI Commands
 * M3 - State persistence management (prune, export, import)
 */
import type { Logger } from '../utils/logger.js';
export interface CapsulePruneOptions {
    basePath?: string;
    apply?: boolean;
    keepDays?: number;
    yes?: boolean;
    verbose?: boolean;
}
export interface CapsuleExportOptions {
    basePath?: string;
    capsule?: string;
    kind?: string;
    out?: string;
    verbose?: boolean;
}
export interface CapsuleImportOptions {
    basePath?: string;
    file: string;
    overwrite?: boolean;
    verbose?: boolean;
}
/**
 * Prune old capsules based on retention policy
 */
export declare function runCapsulePrune(logger: Logger, options?: CapsulePruneOptions): Promise<void>;
/**
 * Export capsules to NDJSON file
 */
export declare function runCapsuleExport(logger: Logger, options: CapsuleExportOptions): Promise<void>;
/**
 * Import capsules from NDJSON file
 */
export declare function runCapsuleImport(logger: Logger, options: CapsuleImportOptions): Promise<void>;
//# sourceMappingURL=capsule.d.ts.map