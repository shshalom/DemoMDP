/**
 * Policy CLI Commands - init, build, sign, verify
 *
 * Implements Build Step 10 policy authoring and compilation flow.
 */
import type { Logger } from '../utils/logger.js';
export interface PolicyInitOptions {
    id: string;
    scope: string;
    out: string;
    verbose?: boolean;
}
export interface PolicyBuildOptions {
    in?: string;
    out?: string;
    channel?: string;
    basePath?: string;
    version?: string;
    strict?: boolean;
    verbose?: boolean;
    noAi?: boolean;
}
export interface PolicySignOptions {
    in: string;
    out: string;
    keyFile: string;
    verbose?: boolean;
}
export interface PolicyVerifyOptions {
    in: string;
    keyFile?: string;
    basePath?: string;
    verbose?: boolean;
}
export interface KeysGenerateOptions {
    out: string;
    name?: string;
    verbose?: boolean;
}
/**
 * Initialize a new policy file with scaffold
 */
export declare function runPolicyInit(logger: Logger, options: PolicyInitOptions): Promise<void>;
/**
 * Build policies from Markdown to unsigned JSON
 */
export declare function runPolicyBuild(logger: Logger, options: PolicyBuildOptions): Promise<void>;
/**
 * Sign compiled policies with ed25519
 */
export declare function runPolicySign(logger: Logger, options: PolicySignOptions): Promise<void>;
/**
 * Verify signed policies
 */
export declare function runPolicyVerify(logger: Logger, options: PolicyVerifyOptions): Promise<void>;
/**
 * Generate ed25519 keypair
 */
export declare function runKeysGenerate(logger: Logger, options: KeysGenerateOptions): Promise<void>;
//# sourceMappingURL=policy.d.ts.map