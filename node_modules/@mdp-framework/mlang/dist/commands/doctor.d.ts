import type { Logger } from '../utils/logger.js';
export interface DoctorCheckResult {
    name: string;
    passed: boolean;
    message: string;
    required: boolean;
}
export interface DoctorOptions {
    ci?: boolean;
    verbose?: boolean;
}
export declare function runDoctor(logger: Logger, options?: DoctorOptions): Promise<DoctorCheckResult[]>;
//# sourceMappingURL=doctor.d.ts.map