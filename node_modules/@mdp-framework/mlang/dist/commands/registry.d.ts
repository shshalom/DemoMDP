/**
 * Registry CLI Commands
 *
 * Commands:
 * - mlang registry head <channel>
 * - mlang registry get <id> [version]
 * - mlang registry verify <file>
 *
 * @since M2 (Team A)
 */
import type { Logger } from '../utils/logger.js';
export interface RegistryHeadOptions {
    channel?: 'stable' | 'preview' | 'dev';
    basePath?: string;
    verbose?: boolean;
}
export interface RegistryGetOptions {
    version?: string;
    basePath?: string;
    save?: string;
    verbose?: boolean;
}
export interface RegistryVerifyOptions {
    basePath?: string;
    verbose?: boolean;
}
/**
 * Show latest version on channel
 *
 * Usage: mlang registry head <id> [--channel stable] [--base-path .mlang]
 */
export declare function runRegistryHead(logger: Logger, id: string, options?: RegistryHeadOptions): Promise<void>;
/**
 * Fetch policy by ID
 *
 * Usage: mlang registry get <id> [version] [--base-path .mlang] [--save path]
 */
export declare function runRegistryGet(logger: Logger, id: string, options?: RegistryGetOptions): Promise<void>;
/**
 * Verify policy signature
 *
 * Usage: mlang registry verify <file> [--base-path .mlang]
 */
export declare function runRegistryVerify(logger: Logger, file: string, options?: RegistryVerifyOptions): Promise<void>;
/**
 * List all policies in registry
 *
 * Usage: mlang registry list [prefix] [--base-path .mlang]
 */
export declare function runRegistryList(logger: Logger, prefix?: string, options?: {
    basePath?: string;
    verbose?: boolean;
}): Promise<void>;
//# sourceMappingURL=registry.d.ts.map