/**
 * PR Commands (M7)
 *
 * Commands for working with Pull Requests in CI/CD environments
 */
import type { Logger } from '../utils/logger.js';
interface PRReviewOptions {
    pr: string;
    repo?: string;
    checkName?: string;
    basePath?: string;
    verbose?: boolean;
    emitEnvelope?: string;
    dryRun?: boolean;
}
/**
 * Run PR review command
 */
export declare function runPRReview(logger: Logger, options: PRReviewOptions): Promise<void>;
export {};
//# sourceMappingURL=pr.d.ts.map