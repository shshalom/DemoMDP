/**
 * Gateway command - Run gateway in MCP server mode
 */
import type { Logger } from '../utils/logger.js';
export interface GatewayOptions {
    mode?: 'mcp';
    stdio?: boolean;
    http?: boolean;
    port?: number;
    basePath?: string;
    verbose?: boolean;
}
export declare function runGateway(logger: Logger, options: GatewayOptions): Promise<void>;
//# sourceMappingURL=gateway.d.ts.map