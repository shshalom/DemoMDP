/**
 * Memory Tools - MCP tools for querying state capsules
 */
import type { CapsuleStore } from '@mdp-framework/gateway/store';
import type { MCPToolHandler, MCPToolResponse, JSONSchema } from '../protocol/types.js';
/**
 * Memory Get Tool
 *
 * Get a state capsule by ID
 */
export declare class MemoryGetTool implements MCPToolHandler {
    private store;
    name: string;
    description: string;
    inputSchema: JSONSchema;
    constructor(store: CapsuleStore);
    handle(params: {
        id: string;
    }): Promise<MCPToolResponse>;
}
/**
 * Memory Search Tool
 *
 * Search capsules by query (semantic search)
 */
export declare class MemorySearchTool implements MCPToolHandler {
    private store;
    name: string;
    description: string;
    inputSchema: JSONSchema;
    constructor(store: CapsuleStore);
    handle(params: {
        query: string;
        filters?: {
            kind?: 'feature' | 'issue' | 'pr' | 'investigation' | 'custom';
            team?: string;
        };
        limit?: number;
    }): Promise<MCPToolResponse>;
}
//# sourceMappingURL=memory-tools.d.ts.map