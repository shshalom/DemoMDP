/**
 * Policy Tools - MCP tools for querying policies
 */
import type { PolicyResolver } from '@mdp-framework/gateway';
import type { MCPToolHandler, MCPToolResponse, JSONSchema } from '../protocol/types.js';
/**
 * Policy Get Tool
 *
 * Get a policy by ID and optional version
 */
export declare class PolicyGetTool implements MCPToolHandler {
    private resolver;
    name: string;
    description: string;
    inputSchema: JSONSchema;
    constructor(resolver: PolicyResolver);
    handle(params: {
        id: string;
        version?: string;
    }): Promise<MCPToolResponse>;
}
/**
 * Policy Search Tool
 *
 * Search policies by tags, owner, or scope
 */
export declare class PolicySearchTool implements MCPToolHandler {
    private resolver;
    name: string;
    description: string;
    inputSchema: JSONSchema;
    constructor(resolver: PolicyResolver);
    handle(params: {
        tags?: string[];
        owner?: string;
        scope?: 'repo' | 'org' | 'team' | 'global';
        limit?: number;
    }): Promise<MCPToolResponse>;
}
//# sourceMappingURL=policy-tools.d.ts.map