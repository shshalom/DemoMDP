/**
 * Evidence Tools - MCP tools for recording evidence in capsules
 */
import type { CapsuleStore } from '@mdp-framework/gateway/store';
import type { MCPToolHandler, MCPToolResponse, JSONSchema } from '../protocol/types.js';
/**
 * Evidence Record Tool
 *
 * Attach evidence to a state capsule
 */
export declare class EvidenceRecordTool implements MCPToolHandler {
    private store;
    name: string;
    description: string;
    inputSchema: JSONSchema;
    constructor(store: CapsuleStore);
    handle(params: {
        capsule_id: string;
        evidence: {
            type: 'file' | 'link' | 'artifact' | 'log' | 'custom';
            ref: string;
            title?: string;
            metadata?: Record<string, unknown>;
        };
    }): Promise<MCPToolResponse>;
}
//# sourceMappingURL=evidence-tools.d.ts.map