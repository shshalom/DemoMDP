/**
 * MCP Transport Layer - stdio and HTTP transports
 */
import type { JSONRPCRequest, JSONRPCResponse } from './types.js';
/**
 * Transport interface
 */
export interface Transport {
    /**
     * Start the transport
     */
    start(): Promise<void>;
    /**
     * Stop the transport
     */
    stop(): Promise<void>;
    /**
     * Check if transport is running
     */
    isRunning(): boolean;
}
/**
 * stdio Transport
 *
 * Reads JSON-RPC requests from stdin and writes responses to stdout
 */
export declare class StdioTransport implements Transport {
    private running;
    private handleRequest;
    constructor(handleRequest: (request: JSONRPCRequest) => Promise<JSONRPCResponse>);
    start(): Promise<void>;
    stop(): Promise<void>;
    isRunning(): boolean;
    private sendResponse;
}
/**
 * HTTP Transport
 *
 * Handles JSON-RPC requests over HTTP POST
 */
export declare class HTTPTransport implements Transport {
    private server?;
    private running;
    private handleRequest;
    private port;
    constructor(port: number, handleRequest: (request: JSONRPCRequest) => Promise<JSONRPCResponse>);
    start(): Promise<void>;
    stop(): Promise<void>;
    isRunning(): boolean;
    getPort(): number;
}
//# sourceMappingURL=transport.d.ts.map