/**
 * @mdp-framework/agents - Engineer Agent
 *
 * Engineer agent for implementation planning and technical execution
 */
import type { StateCapsule } from '@mdp-framework/schemas';
import type { AIProvider } from '@mdp-framework/ai-services';
import { BaseAgent, type AgentResult } from '../base/agent.js';
/**
 * Engineer Decision output structure
 */
export interface EngineerDecision {
    implementation_assessment: {
        feasibility: 'straightforward' | 'moderate' | 'complex' | 'challenging';
        effort_estimate: string;
        complexity_factors?: string[];
        recommendations?: string;
    };
    tasks: Array<{
        id: string;
        title: string;
        description: string;
        component: string;
        effort_estimate?: string;
        dependencies?: string[];
        priority?: 'high' | 'medium' | 'low';
    }>;
    implementation_decisions: Array<{
        question: string;
        answer: string;
        reasoning: string;
        code_example?: string;
        confidence?: number;
    }>;
    code_structure?: Array<{
        file_path: string;
        purpose: string;
        type: 'create' | 'modify';
        interfaces?: string[];
        dependencies?: string[];
    }>;
    testing_strategy?: {
        unit_tests?: string[];
        integration_tests?: string[];
        test_coverage_target?: string;
        testing_notes?: string;
    };
    risks?: Array<{
        category: 'complexity' | 'unknown' | 'performance' | 'testing';
        severity: 'high' | 'medium' | 'low';
        description: string;
        mitigation?: string;
    }>;
    technical_notes?: string[];
    next_steps?: string[];
}
/**
 * Engineer Agent Configuration
 */
export interface EngineerAgentConfig {
    /**
     * AI provider for LLM completions
     */
    aiProvider: AIProvider;
    /**
     * Optional MCP client for code analysis
     * TODO: Integrate with Stream 3 MCP Server when available
     */
    mcpClient?: any;
    /**
     * Model to use (default: claude-sonnet-4.5)
     */
    model?: string;
    /**
     * Temperature for LLM (default: 0.1 for determinism)
     */
    temperature?: number;
    /**
     * Max tokens for response
     */
    maxTokens?: number;
}
/**
 * Engineer Agent
 *
 * Reviews architecture design, assesses implementation complexity,
 * plans tasks, and makes implementation-level decisions.
 */
export declare class EngineerAgent extends BaseAgent {
    private aiProvider;
    private mcpClient?;
    private model;
    private temperature;
    private maxTokens;
    constructor(config: EngineerAgentConfig);
    /**
     * Execute implementation planning
     */
    protected executeImpl(capsule: StateCapsule): Promise<Omit<AgentResult, 'agent' | 'execution_time_ms'>>;
    /**
     * Get next agent role (Designer, optional)
     */
    protected getNextAgentRole(): string | null;
    /**
     * Extract context from capsule and previous agent decisions
     */
    private extractContext;
    /**
     * Build analysis prompt with context
     */
    private buildAnalysisPrompt;
    /**
     * Convert engineer decision to agent decisions
     */
    private convertToDecisions;
    /**
     * Create artifacts from engineer decision
     */
    private createArtifacts;
}
//# sourceMappingURL=engineer-agent.d.ts.map