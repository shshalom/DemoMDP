/**
 * @mdp-framework/agents - Blackboard Pattern
 *
 * Implements shared state coordination for multi-agent systems
 *
 * Contract: Blackboard interface is FROZEN per STREAM-5-MULTI-AGENT.md
 */
import type { Agent, Decision, Artifact } from './agent.js';
/**
 * Blackboard interface for shared state coordination
 *
 * CONTRACT: FROZEN - changes require major version bump
 */
export interface Blackboard {
    /**
     * Write decision to shared state
     */
    writeDecision(agent: string, decision: Decision): void;
    /**
     * Read all decisions
     */
    readDecisions(): Decision[];
    /**
     * Write artifact to shared state
     */
    writeArtifact(agent: string, artifact: Artifact): void;
    /**
     * Read all artifacts
     */
    readArtifacts(): Artifact[];
    /**
     * Get context summary for agent
     */
    getContextFor(agent: Agent): string;
}
/**
 * In-memory implementation of the Blackboard pattern
 *
 * Provides thread-safe coordination for multi-agent workflows
 */
export declare class InMemoryBlackboard implements Blackboard {
    private decisions;
    private artifacts;
    /**
     * Write decision to shared state
     */
    writeDecision(agent: string, decision: Decision): void;
    /**
     * Read all decisions
     */
    readDecisions(): Decision[];
    /**
     * Get decisions by agent
     */
    getDecisionsByAgent(agentName: string): Decision[];
    /**
     * Write artifact to shared state
     */
    writeArtifact(agent: string, artifact: Artifact): void;
    /**
     * Read all artifacts
     */
    readArtifacts(): Artifact[];
    /**
     * Get artifacts by agent
     */
    getArtifactsByAgent(agentName: string): Artifact[];
    /**
     * Get context summary for agent
     *
     * Provides a formatted summary of relevant decisions and artifacts
     */
    getContextFor(agent: Agent): string;
    /**
     * Get role-specific context hints
     */
    private getRoleSpecificContext;
    /**
     * Clear all data (useful for testing)
     */
    clear(): void;
    /**
     * Get statistics about the blackboard
     */
    getStats(): {
        totalDecisions: number;
        totalArtifacts: number;
        agentCounts: Record<string, {
            decisions: number;
            artifacts: number;
        }>;
    };
}
//# sourceMappingURL=blackboard.d.ts.map